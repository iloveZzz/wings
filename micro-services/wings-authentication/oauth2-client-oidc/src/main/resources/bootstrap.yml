spring:
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: wings-config-server
      name: common,authentication
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    prefer-same-zone-eureka: true
    region: beijing
