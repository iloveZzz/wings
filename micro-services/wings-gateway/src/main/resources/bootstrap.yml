spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: wings-config-server
      name: gateway
      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/